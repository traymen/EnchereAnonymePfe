package com.aymen.enchere.ServiceKeyclock;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.keycloak.KeycloakSecurityContext;
/*
@Service
public class UserService {

    private KeycloakSecurityContext keycloakSecurityContext;

    public String getUsername() {
        return keycloakSecurityContext.getToken().getPreferredUsername();
    }

    public String getEmail() {
        return keycloakSecurityContext.getToken().getEmail();
    }
    public String getNom() {
        return keycloakSecurityContext.getToken().getNickName();
    }

    // Autres méthodes pour récupérer les données de l'utilisateur
}
*/